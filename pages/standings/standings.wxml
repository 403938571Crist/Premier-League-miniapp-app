<view class="container">
  <view class="tab-bar">
    <view
      wx:for="{{tabs}}"
      wx:key="key"
      class="tab-item {{activeTab === item.key ? 'active' : ''}}"
      bindtap="switchTab"
      data-type="{{item.key}}">
      {{item.label}}
    </view>
  </view>

  <loading-state wx:if="{{loading}}" class="flex-fill"></loading-state>

  <error-state wx:elif="{{error}}" class="flex-fill" bind:retry="loadData"></error-state>

  <swiper
    wx:else
    class="rank-swiper"
    current="{{currentTabIndex}}"
    duration="280"
    bindchange="onSwiperChange">

    <!-- 总积分榜 -->
    <swiper-item>
      <scroll-view class="pane-scroll" scroll-y enhanced show-scrollbar="{{false}}">
        <view class="swiper-pane">
          <view class="table-card">
            <view class="table-header">
              <view class="header-position">排名</view>
              <view class="header-team">球队</view>
              <view class="header-stats">
                <view class="header-stat">赛</view>
                <view class="header-stat">净</view>
                <view class="header-stat points">分</view>
              </view>
            </view>

            <view class="cache-tip" wx:if="{{activeTab === 'TOTAL' && isCached}}">
              <text>数据来自缓存 · {{cacheTime}}</text>
            </view>

            <view class="standings-list" wx:if="{{standings.length > 0}}">
              <team-item
                wx:for="{{standings}}"
                wx:key="position"
                team="{{item}}"
                bind:tap="onTeamTap">
              </team-item>
            </view>

            <empty-state
              wx:else
              title="{{tabs[0].emptyTitle}}"
              description="{{tabs[0].emptyDescription}}"
              show-action="{{true}}"
              action-text="重新加载"
              bind:action="loadData">
            </empty-state>
          </view>
        </view>

        <view class="legend">
          <view class="legend-item">
            <view class="legend-color champion"></view>
            <text class="legend-text">冠军</text>
          </view>
          <view class="legend-item">
            <view class="legend-color champions-league"></view>
            <text class="legend-text">欧冠区</text>
          </view>
          <view class="legend-item">
            <view class="legend-color europa-league"></view>
            <text class="legend-text">欧战区</text>
          </view>
          <view class="legend-item">
            <view class="legend-color relegation"></view>
            <text class="legend-text">降级区</text>
          </view>
        </view>

        <view class="data-source">
          <text>数据来源于后端 /api 聚合服务</text>
        </view>
      </scroll-view>
    </swiper-item>

    <!-- 射手榜 -->
    <swiper-item>
      <scroll-view class="pane-scroll" scroll-y enhanced show-scrollbar="{{false}}">
        <view class="swiper-pane">
          <view class="table-card">
            <view class="player-table-header">
              <view class="player-rank-head">排名</view>
              <view class="player-name-head">球员</view>
              <view class="player-number-head main">进球</view>
              <view class="player-number-head">助攻</view>
            </view>

            <view class="cache-tip" wx:if="{{activeTab === 'SCORERS' && isCached}}">
              <text>数据来自缓存 · {{cacheTime}}</text>
            </view>

            <view class="player-rank-list" wx:if="{{scorers.length > 0}}">
              <view class="player-rank-row" wx:for="{{scorers}}" wx:key="playerId">
                <view class="player-rank {{index === 0 || index === 1 || index === 2 ? 'top' : ''}}">{{item.rank}}</view>
                <image class="player-avatar" src="{{item.photoUrl}}" mode="aspectFill" binderror="onAvatarError" data-list="scorers" data-idx="{{index}}"></image>
                <view class="player-info">
                  <text class="player-name">{{item.playerName}}</text>
                  <text class="player-meta">{{item.teamName}} · {{item.position}}</text>
                </view>
                <view class="player-stat main">{{item.goals}}</view>
                <view class="player-stat">{{item.assists}}</view>
              </view>
            </view>

            <empty-state
              wx:else
              title="{{tabs[1].emptyTitle}}"
              description="{{tabs[1].emptyDescription}}"
              show-action="{{true}}"
              action-text="重新加载"
              bind:action="loadData">
            </empty-state>
          </view>
        </view>

        <view class="data-source">
          <text>数据来源于后端 /api 聚合服务</text>
        </view>
      </scroll-view>
    </swiper-item>

    <!-- 助攻榜 -->
    <swiper-item>
      <scroll-view class="pane-scroll" scroll-y enhanced show-scrollbar="{{false}}">
        <view class="swiper-pane">
          <view class="table-card">
            <view class="player-table-header">
              <view class="player-rank-head">排名</view>
              <view class="player-name-head">球员</view>
              <view class="player-number-head main">助攻</view>
              <view class="player-number-head">进球</view>
            </view>

            <view class="cache-tip" wx:if="{{activeTab === 'ASSISTS' && isCached}}">
              <text>数据来自缓存 · {{cacheTime}}</text>
            </view>

            <view class="player-rank-list" wx:if="{{assists.length > 0}}">
              <view class="player-rank-row" wx:for="{{assists}}" wx:key="playerId">
                <view class="player-rank {{index === 0 || index === 1 || index === 2 ? 'top' : ''}}">{{item.rank}}</view>
                <image class="player-avatar" src="{{item.photoUrl}}" mode="aspectFill" binderror="onAvatarError" data-list="assists" data-idx="{{index}}"></image>
                <view class="player-info">
                  <text class="player-name">{{item.playerName}}</text>
                  <text class="player-meta">{{item.teamName}} · {{item.position}}</text>
                </view>
                <view class="player-stat main">{{item.assists}}</view>
                <view class="player-stat">{{item.goals}}</view>
              </view>
            </view>

            <empty-state
              wx:else
              title="{{tabs[2].emptyTitle}}"
              description="{{tabs[2].emptyDescription}}"
              show-action="{{true}}"
              action-text="重新加载"
              bind:action="loadData">
            </empty-state>
          </view>
        </view>

        <view class="data-source">
          <text>数据来源于后端 /api 聚合服务</text>
        </view>
      </scroll-view>
    </swiper-item>

  </swiper>
</view>
